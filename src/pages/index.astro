---
import { getCollection } from "astro:content";
import PageLayout from "@/layouts/Base";
import PostPreview from "@/components/blog/PostPreview";
import SocialList from "@/components/SocialList";
import { sortMDByDate } from "@/utils";

const MAX_POSTS = 10;
const allPosts = await getCollection("post");
const allPostsByDate = sortMDByDate(allPosts).slice(0, MAX_POSTS);

const cactusTech: Array<{ title: string; desc: string; href: string }> = [
	{
		title: "Youtube",
		desc: "Discover some Cybersecurity, Blog, and General Videos",
		href: "https://www.youtube.com/@peppersecurity",
	},
	{
		title: "LinkedIn",
		desc: "Connect with my professional side!",
		href: "https://www.linkedin.com/in/tonyvt/",
	},
	{
		title: "Twitter",
		desc: "Check out my Twitter!",
		href: "https://twitter.com/pepsecurity",
	},
	{
		title: "Github",
		desc: "Check out my Gits, Repos, Etc.",
		href: "https://github.com/linpep",
	},
	
];
---

<PageLayout meta={{ title: "Home" }}>
	<section>
		<h1 class="title mb-6">// Hi, I'm Tony!</h1>
		<p class="mb-5">
			I am currently a Cybersecurity student developing my skills in Pen-Testing, Development, Security Research, Network Administration, Systems Administration, Cloud Security and more!
		</p>
		<p>
			I currently am the program leader in my Cybersecurity program at my High School! I also am proud to say and call myself a Cybersecurity instructor and mentor of sorts.
		</p>
	</section>
	<section aria-label="Blog post list" class="mt-16">
		<h2 class="title mb-4 text-xl">// Blog Posts</h2>
		<ul class="space-y-4 sm:space-y-2">
			{
				allPostsByDate.map((p) => (
					<li class="flex flex-col gap-x-2 sm:flex-row">
						<PostPreview post={p} />
					</li>
				))
			}
		</ul>
	</section>
	<section class="mt-16">
		<h2 class="title mb-4 text-xl">// Find me on</h2>
		<ul class="space-y-4 sm:space-y-2">
			{
				cactusTech.map(({ href, title, desc }) => (
					<li>
						<a
							href={href}
							target="_blank"
							rel="noopener noreferrer"
							class="cactus-link inline-block"
						>
							{title}
						</a>: 
						<p class="inline-block sm:mt-2">{desc}</p>
					</li>
				))
			}
			
		</ul>
	</section>
</PageLayout>
